"""Orchestration layer for ESG compliance agents.

- ``supervisor_agent.py`` — deterministic regex routing (default)
- ``planner_agent.py`` — LLM task decomposition + dynamic MCP tool selection
- ``agent.py`` (repo root) — full Pydantic AI agent with progressive skill loading
"""
