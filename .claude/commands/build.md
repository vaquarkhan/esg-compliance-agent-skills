Execute runnable examples and copy attested artifacts to `artifacts/build/`.

Run: `python -m scripts.lifecycle.build`

Outputs: `ghg_inventory.json`, `factor_provenance.json`, `mapping_matrix.json`, `build_manifest.json` — each with `pending_sustainability_assurance_sign_off`.
