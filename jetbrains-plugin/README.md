# ESG Compliance Agent Skills — JetBrains plugin scaffold

Build with JDK 17+:

```powershell
cd jetbrains-plugin
.\gradlew.bat buildPlugin
```

Output: `build/distributions/ESG Compliance Agent Skills-1.0.0.zip`

See [docs/plugin-publishing.md](../docs/plugin-publishing.md).
