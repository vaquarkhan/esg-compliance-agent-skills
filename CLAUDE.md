# Claude Code entry — see AGENTS.md for routing

Read [AGENTS.md](AGENTS.md) before any ESG compliance task.

Lifecycle: `/spec` → `/plan` → `/build` → `/validate` → `/review` → `/ship`

Human approval required for filings and legal conclusions.
